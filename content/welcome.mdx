---
title: "My Blog Post"
author: "John Doe"
date: "2024-01-15"
---

# My Blog Post[Subtitle]

This is a regular paragraph with **bold** and _italic_ text.

## Code Examples

Here's some inline code: `console.log('hello')`

```javascript
// This will be syntax highlighted with Shiki
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("Next.js"); // [!code highlight]
```

```typescript {1,3-5} title="Advanced Example"
interface User {
  name: string; // [!code ++]
  age: number; // [!code --]
  email: string; // [!code focus]
}

const user: User = {
  name: "John",
  email: "john@example.com",
};
```

## Custom Components

<Callout type="warning">
  This is a warning callout using the custom component!
</Callout>

<Steps>

### Step 1: Install dependencies

First, install the required packages.

### Step 2: Configure Next.js

Update your `next.config.mjs` file.

### Step 3: Create MDX files

Start writing your content!

</Steps>

<CodeGroup>

```ts app [example.ts]
import { client } from "./client.js";

const blockNumber = await client.getBlockNumber();
```

```ts app [client.ts] filename="client.ts"
import { http, createPublicClient } from "viem";
import { mainnet } from "viem/chains";

export const client = createPublicClient({
  chain: mainnet,
  transport: http(),
});
```

</CodeGroup>

## Links and Images

[Link to Next.js](https://nextjs.org)

> This is a blockquote with some important information.

- List item 1
- List item 2
- List item 3

1. Numbered item 1
2. Numbered item 2
3. Numbered item 3
